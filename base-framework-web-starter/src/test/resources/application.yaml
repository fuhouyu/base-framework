spring:
  application:
    name: test
base:
  framework:
    cache:
      service:
        cache-service-type: caffeine
    kms-national:
      sm2:
        auto-generate: true
    ip2region:
      enabled: true
      db-path: classpath:ip2region.xdb